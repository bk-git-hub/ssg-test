export type User = {
  id: string;
  name: string;
  nickname: string;
  profileImageUrl: string;
};

export type UserDetail = {
  id: string;
  name: string;
  profileImageUrl: string;
  projectCount: number;
  articleCount: number;
};
